# Netlify configuration file for Workers United

[build]
  publish = "."

[build.environment]
  NODE_VERSION = "18"

[forms]
  # Netlify Forms automatically handles forms with data-netlify="true"

[context.production]
  command = ""

[context.deploy-preview]
  command = ""
